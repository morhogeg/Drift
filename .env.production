# Production build env (loaded by `vite build`). Committed on purpose as a guard:
# it pins the API key to EMPTY so no key can ever be inlined into a release
# bundle, even if a key accidentally ends up in .env. Production is BYOK.
VITE_GEMINI_API_KEY=
VITE_OPENROUTER_API_KEY=
