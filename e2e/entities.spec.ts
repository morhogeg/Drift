// Placeholder E2E spec (Playwright/Cypress-like pseudo)
// Verify: hover preview -> jump back -> forward

describe('Entity navigation (pseudo)', () => {
  it('shows preview on hover and navigates', () => {
    // This is a placeholder file to outline E2E intent.
    // Implement with your chosen E2E framework.
  })
})

